/*extern crate check_argument_cargo_io; 

#[test]
fn check_false () {
    let a_string = String::from ("hi");
    let b_string = String::from("ni");
    let test_list: Vec<String> = vec![a_string, b_string];
	assert_eq!(check_argument_cargo_io::valid_the_input::check_control(& test_list).response, false);
	assert_eq!(check_argument_cargo_io::valid_the_input::check_control(& test_list).err_loc, 1);
    //assert_eq!(check_argument_cargo_io::valid_the_input::check_control(& test_list), {false, 1});
}

#[test]
fn check_true () {
    let a_string = String::from ("hi");
    let b_string = String::from("ni");
    let c_string = String::from("hao.txt");
    let test_list: Vec<String> = vec![a_string, b_string, c_string];
	assert_eq!(check_argument_cargo_io::valid_the_input::check_control(& test_list).response, true);
	assert_eq!(check_argument_cargo_io::valid_the_input::check_control(& test_list).err_loc, 0);
    //assert_eq!(check_argument_cargo_io::valid_the_input::check_control(& test_list), {true, 0});
}*/
